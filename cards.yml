server:
  port: 8090
  
spring:
  application:
    name: "cards"
  datasource:
    url: jdbc:mysql://localhost:3308/cardsdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always

build:
  version: 1.1

loans:
  contact:
    name: "dev"
    email: "dev-cards@dev.com"
  message: "This is Cards service using dev env(Default)."
